
<h1><%= @list.name %>:</h1>

<% @list.items.each do |item| %>
	<p><%= item.name %></p>
	<p><%= item.description %></p>
<% end %>


<br />

<%= link_to 'Add Task', new_item_path(list_id: @list.id) %>
<%= link_to 'Edit', edit_list_path(@list) %>
<%= link_to 'Delete', list_path(@list), method: :delete %>
<%= link_to 'Back to Lists', lists_path %>
