<%= form_for [@item] do |f| %>
	<%= f.label :name %>
	<%= f.text_field :name %>

	<%= f.label :description %>
	<%= f.text_field :description %>
	<%= f.hidden_field :list_id, :value => @list.id %>
	<%= f.submit %>
<% end %>