<!-- Index should be within specific list -->
<h1>To-Do Items</h1>
<p><%= link_to 'New Task', new_item_path %></p>
<% @items.each do |item| %>
	<h2><%= item.name %></h2>
<p><%= link_to 'View Task', item_path(item)%></p>
<% end %> 